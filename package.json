{
  "private": true,
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test --continue"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "turbo": "^1.5.5"
  }
}
